import { Vujahday_Script } from 'next/font/google';

// Xera accent display font (handwritten/script) used for emphasis in headings
export const vujahdayScript = Vujahday_Script({
  subsets: ['latin'],
  weight: '400',
  display: 'swap',
  variable: '--font-xera-accent',
});
